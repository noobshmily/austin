# HTTP 连接配置 T0D0
ok:
  http:
    connect-timeout: 30
    keep-alive-duration: 300
    max-idle-connections: 200
    read-timeout: 30
    write-timeout: 30

# 腾讯云账号相关的信息配置 TODO
tencent:
  sms:
    account:
      secret-id:
      secret-key:
      sign_name:
      sms-sdk-app-id:
      template-id:


# 数据库相关的信息配置 TODO
spring:
  datasource:
    url: jdbc:mysql://150.158.50.190:3306/austin?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: '@Zby746459'
    driver-class-name: com.mysql.jdbc.Driver
  # kafka相关的信息配置 TODO
  kafka:
    bootstrap-servers: 150.158.50.190:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      auto:
        offset:
          reset: earliest
      auto-commit-interval: 1000
      enable-auto-commit: true


  output:
    ansi:
      enabled: always
# tomcat / HikariPool(数据库连接池 配置） TODO
# 消息topicName TODO
austin:
  topic:
    name: austin